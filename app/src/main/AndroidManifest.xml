<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light"
        tools:targetApi="31">

        <activity
            android:name=".ShoppingItemEdit"
            android:label="EditItem"
            android:theme="@style/Theme.AppCompat.Light">

        </activity>

        <activity
            android:name=".NotesActivity"
            android:label="NotesActivity"
            android:theme="@style/Theme.AppCompat.Light">

        </activity>

        <activity
            android:name=".ChooseShoppingItem"
            android:label="ChooseShoppingItem"
            android:theme="@style/Theme.AppCompat.Light">

        </activity>

        <activity android:name=".AddedItemsDialoge"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        </activity>

        <activity android:name=".NoteItemEdit"
            android:label="EditNote"
            android:theme="@style/Theme.AppCompat.Light">

        </activity>

        <activity
            android:name=".ShoppingListActivity"
            android:exported="true"
            android:theme="@style/Theme.ShoppingAndNotes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>